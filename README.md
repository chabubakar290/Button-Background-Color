In this task i learn how to use a button with javascript to change the background color of a webpage.
In The test, I use three ways to add events: onclick, addEventListener(), and inline HTML.
onclick is simple, addEventListener() is more flexible and useful for real projects,
while inline HTML is easy but mixes HTML and JavaScript.
In the challenge i use if-else and a Boolean variable to toggle the background between red and blue on every click.
